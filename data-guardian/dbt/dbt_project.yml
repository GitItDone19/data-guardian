name: 'data_guardian_dbt'
version: '1.0.0'
config-version: 2

profile: 'data_guardian_profile'

model-paths: ["models"]
analysis-paths: ["analyses"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]
snapshot-paths: ["snapshots"]

clean-targets:
  - "target"
  - "dbt_packages"

models:
  data_guardian_dbt:
    staging:
      +schema: staging
      +materialized: view
    core:
      +schema: core
      +materialized: table
